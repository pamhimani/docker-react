sudo: required
services: 
  - docker

before_install:
  - docker build -t pamhimani/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pamhimani/docker-react npm run test -- --coverage